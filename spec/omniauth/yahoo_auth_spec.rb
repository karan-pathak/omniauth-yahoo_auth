require 'spec_helper'

describe Omniauth::YahooAuth do
  it 'has a version number' do
    expect(Omniauth::YahooAuth::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
